

const visual_tasks = require('@offirmo/cli-toolbox/stdout/visual_tasks')

visual_tasks.run([
	{
		title: 'Gather list of models',
		task: () => {

		}
	},
	{
		title: 'Syncronize models',
		task: () => {

		}
	}
])
